<section class="card-container shadow">
    @if (TitleContent is not null)
    {
        @TitleContent
    }
    @if (ShowHeaderWithAddButton)
    {
        <div class="card-header-simple">
            <h3>@HeaderTitle</h3>
            <button class="btn-add" @onclick="OnAddButtonClick">+</button>
        </div>
    }
    @if (Title != string.Empty && !ShowHeaderWithAddButton)
    {
        <h1 class="cd-title">@Title</h1>
    }
    @if (IsLoading)
    {
    }
    else
    {
        <div class="container">
            @ChildContent
        </div>
    }
    @if (Reload is not null)
    {
    }
</section>
@code {
    /// <summary>
    /// Gets or sets the child content to be displayed within the card
    /// </summary>
    [Parameter]
    public required RenderFragment ChildContent { get; set; }
    /// <summary>
    /// Gets or sets a value indicating whether the card is in a loading state
    /// When true, displays a skeleton loader instead of content
    /// </summary>
    [Parameter]
    public bool IsLoading { get; set; } = false;
    /// <summary>
    /// Gets or sets the title displayed at the top of the card
    /// If empty, no title is shown
    /// </summary>
    [Parameter]
    public string Title { get; set; } = string.Empty;

    /// <summary>
    /// The height of the card in pixels. Default is 300.  
    /// </summary>
    [Parameter]
    public int Height { get; set; } = 300;

    [Parameter]
    public Action? TitleActions { get; set; }

	private string _heightString => Height > 0 ? $"{Height}px" : "300px";

    /// <summary>
    /// Gets or sets the title content to be displayed within the card
    /// </summary>
    [Parameter]
    public RenderFragment? TitleContent { get; set; }

    /// <summary>
    /// Optionally pass a function to reload the card content
    /// </summary>
    [Parameter]
    public Func<Task>? Reload { get; set; }

    /// <summary>
    /// Gets or sets whether to show a header with an add button
    /// </summary>
    [Parameter]
    public bool ShowHeaderWithAddButton { get; set; } = false;

    /// <summary>
    /// Gets or sets the title for the header with add button
    /// </summary>
    [Parameter]
    public string HeaderTitle { get; set; } = string.Empty;

    /// <summary>
    /// Event callback for when the add button is clicked
    /// </summary>
    [Parameter]
    public EventCallback OnAddButtonClick { get; set; }
}

<style>

    .card-container {
        background: var(--color-white);
        padding: 15px;
        border-radius: var(--border-rad);
        overflow-x: scroll !important;
        margin-right: auto;
        margin-left: auto;
        overflow: scroll;
    }

    .cd-title{
        text-align: left;
        margin-bottom: 1.5rem;
        margin-left: 1.5rem;
    }

    .card-header-simple {
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 100%;
        margin-bottom: 1rem;
        padding-bottom: 0.75rem;
        border-bottom: 1px solid #e2e8f0;
    }

    .card-header-simple h3 {
        margin: 0;
        font-size: 1rem;
        font-weight: 700;
        color: var(--color-primary);
    }

    .btn-add {
        background: #10b981;
        color: white;
        border: none;
        padding: 0.4rem 0.8rem;
        border-radius: 6px;
        font-size: 0.85rem;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.2s;
    }

    .btn-add:hover {
        background: #059669;
    }

    .refresh-button {
        position: absolute !important;
        bottom: 0px !important;
        right: 0 !important;
    }

  /*   @@media (min-width: 768px) {
        .card-container {
            width: fit-content;
        }
    }

    @@media (min-width: 900px) {
        .card-container {
            width: 800px;
        }
    }

    @@media (min-width: 992px) {
        .card-container {
            width: 970px;
        }
    }
    @@media (min-width: 1060px) {
        .card-container {
            width: 1000px;
        }
    }
    @@media (min-width: 1200px) {
        .card-container {
            width: auto;
        }
    } */
</style>